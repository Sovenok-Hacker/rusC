# rusC
A little project for renaming C keywords to russian language (it is not second 1С :D 🇷🇺)

# Example:

```c
#include <rusC.h>
числовой главный() {
	печатать("Маленькие совушки по травушке бегут!\n");
}
```
